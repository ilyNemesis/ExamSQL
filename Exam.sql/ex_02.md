10. SELECT * FROM users

11. SELECT name FROM users

12. SELECT * FROM users WHERE age > 25

13. SELECT * FROM users WHERE city = "Paris"

14. comme tout a l'heure je ne me souviens pas exactement du symbole utilisé donc je shoot my shot come michael Jordans (bismilah) 
SELECT * FROM users WHERE name = "%%Mar%%" OR firstname = "%%Mar%%". l'objectif etant de chercher gngngngn avec x char avant ou apres pour le name et le first name (OR parce que l'on veut l'un, l'autre ou les deux, pas uniquement les deux).

15. SELECT * FROM users WHERE (city = "Marseille" OR city = "Lille") AND (age >= 18 AND age <= 30)
en gros on selectionne les habitant de marseille ou lille, et on ajoute une codition avec AND pour trier les ages et renvoie que les users entre 18 et 30. (j'ai abandonné l'idée d'ecrire un francais correct j'ai beaucoup trop mal a la tete et aux yeux)

16. SELECT * FROM users ORBER BY age (on trie par age, en tout cas c'est l'idée que j'ai, maybe la syntaxe est mauvaise)