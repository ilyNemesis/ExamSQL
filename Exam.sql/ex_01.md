1. SELECT * FROM table

2. USE database

3. "SELECT *" séléctionne tout le contenue dans la table donné et renvoie le resultat. "SELECT colonne" selectio.

4. aucune idée, on peut garantir de ne selectionné aucun doublons en faisant appel a la clé primaire de ce que l'on cherche (ex SELECT id FROM users WHERE id = 2), on peut aussi ajouter des conditions pour filtrer le resultat, mais je ne sais pas sinon :/

5. WHERE, comme dit juste au dessus, where permet de filtrer les resultat en ajoutant un condition

6. je me souviens pas, ca fait un petit moment, mais c'etait quelque chose comme %%[valeur]%% pour chercher quelque chose avant. je ne sais juste plus de quel symbole il s'agit. je sais, qu'on a la possibilité de mettre une valeur prédéfinis a remplir (il manque 3 char alors on cherche 3 char) ou mettre un nombre indéfinis (il manque x char)

7. AND renvoyer un resultat basé sur le cas 1 et 2, si 1 et 2 sont vrai il compte comme TRUE (donc affiche quelque chose) si le cas 1, 2 ou les deux sont faux, il compte comme FALSE (donc ne retourne rien).
OR quant a lui a besoin que d'un des 2 cas soit juste pour renvoyer le resultat, sois le cas 1, sois le cas 2, ou les 2, mais ne renvoie rien si les 2 cas sont faux.

8. aucune idée encore une fois, ajouté un ID avec auto_increment et comptez les lignes comme moi :)))

9. DROP sert a oublié une table ou un database, ce qui supprime tout son contenue au passage, DELETE sert juste a supprimé