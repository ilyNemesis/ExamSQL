1. SELECT * FROM table

2. USE database

3. "SELECT *" séléctionne tout le contenue dans la table donné et renvoie le resultat. "SELECT colonne" selectio.

4. aucune idée, on peut garantir de ne selectionné aucun doublons en faisant appel a la clé primaire de ce que l'on cherche (ex SELECT id FROM users WHERE id = 2), on peut aussi ajouter des conditions pour filtrer le resultat, mais je ne sais pas sinon :/

5. WHERE, comme dit juste au dessus, where permet de filtrer les resultat en ajoutant un condition

6. 